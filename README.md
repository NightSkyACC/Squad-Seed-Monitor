# Squad-Seed-Monitor
A lightweight desktop application for Squad players and server administrators. It monitors a specific BattleMetrics server ID and automates system actions (like killing the game process or shutting down the PC) when a target player threshold is reached.
